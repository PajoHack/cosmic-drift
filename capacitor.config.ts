import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.patrickhackman.cosmicdrift',
  appName: 'Cosmic Drift Runner',
  webDir: 'dist'
};

export default config;
